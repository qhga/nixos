#!/usr/bin/env sh

# Required for basic functionality
ln -s $HOME/sync/pw $HOME/.password-store
ln -s $HOME/sync/app_data/emacs/.authinfo.gpg $HOME/
ln -s $HOME/sync/app_data/thunderbird/first $HOME/.thunderbird/first
ln -s $HOME/sync/ssh $HOME/.ssh
ln -s /fast/git $HOME/git